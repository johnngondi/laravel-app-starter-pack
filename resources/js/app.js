// Livewire PowerGrid browser runtime. Registers the Alpine components
// (e.g. `pgRenderActions`) that render row action buttons and other
// interactive grid behaviour. Without this import the Actions column
// stays blank. The matching styles are imported in resources/css/app.css.
import '../../vendor/power-components/livewire-powergrid/dist/powergrid.js';
